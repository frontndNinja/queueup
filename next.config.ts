import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["media.themoviedb.org", "lh3.googleusercontent.com", "image.tmdb.org"],
  },
};

export default nextConfig;
